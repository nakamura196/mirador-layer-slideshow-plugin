// import React from 'react';
// import { shallow } from 'enzyme';
// import { TestableLayerSlideshow as MiradorLayerSlideshow }
// from '../src/plugins/MiradorLayerSlideshow';
// import LayerSlideshow from '../src/plugins/LayerSlideshow';

/*
const mockPalette = {
  palette: {
    shades: { main: 'rgb(255, 255, 255)' },
    getContrastText: () => 'rgb(0, 0, 0)',
  },
};
*/

/*
function createWrapper(props) {
  return shallow(
    <MiradorLayerSlideshow
      classes={{}}
      viewer={{}}
      updateViewport={() => {}}
      updateWindow={() => {}}
      windowId="x"
      width="sm"
      theme={mockPalette}
      t={() => {}}
      {...props}
    />,
  );
}
*/

describe('MiradorLayerSlideshow', () => {
  // let wrapper;
  it('renders LayerSlideshow', () => {
    // wrapper = createWrapper();
    // expect(wrapper.find(LayerSlideshow).length).toBe(5);
  });
});
